{
  "name": "sunio-api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@supabase/ssr": "^0.6.1",
    "hono": "^4.10.3",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
